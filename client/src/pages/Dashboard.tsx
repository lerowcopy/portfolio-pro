import { useState } from "react";
import { FolderPlus, LayoutDashboard, Loader2, Plus, Sparkles } from "lucide-react";
import { useLocation } from "wouter";
import { AppMark } from "@/components/AppMark";
import { PortfolioCard } from "@/components/PortfolioCard";
import { ThemeToggle } from "@/components/ThemeToggle";
import { Button } from "@/components/ui/button";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";

export default function Dashboard() {
  const { user, loading, isAuthenticated, logout } = useAuth();
  const [, setLocation] = useLocation();
  const [creating, setCreating] = useState(false);
  const utils = trpc.useUtils();
  const list = trpc.portfolios.list.useQuery(undefined, { enabled: isAuthenticated });
  const create = trpc.portfolios.create.useMutation({ onSuccess: (portfolio) => { void utils.portfolios.list.invalidate(); setLocation(`/dashboard/portfolios/${portfolio.id}/edit`); } });
  const remove = trpc.portfolios.remove.useMutation({ onSuccess: () => void utils.portfolios.list.invalidate() });

  if (loading) return <div className="grid min-h-svh place-items-center bg-[#fcfcfd] dark:bg-[#090a0f]"><Loader2 className="size-5 animate-spin text-violet-600" /></div>;
  if (!isAuthenticated) return <div className="grid min-h-svh place-items-center bg-[#fcfcfd] p-6 dark:bg-[#090a0f]"><div className="max-w-sm text-center"><AppMark className="justify-center" /><h1 className="mt-8 font-display text-3xl font-semibold tracking-[-0.05em]">Your workspace awaits.</h1><p className="mt-3 text-sm leading-6 text-slate-500 dark:text-slate-400">Sign in to create and manage your published work.</p><Button onClick={() => startLogin()} className="mt-7 rounded-full bg-violet-700 px-5 text-white hover:bg-violet-800">Continue with Manus</Button></div></div>;

  async function createPortfolio() { setCreating(true); try { await create.mutateAsync(); } finally { setCreating(false); } }
  const portfolios = list.data ?? [];

  return <div className="min-h-svh bg-[#f8f8fa] text-slate-950 dark:bg-[#090a0f] dark:text-white"><header className="sticky top-0 z-20 border-b border-slate-200/80 bg-[#f8f8fa]/85 backdrop-blur-xl dark:border-white/10 dark:bg-[#090a0f]/85"><div className="mx-auto flex h-[4.5rem] max-w-7xl items-center justify-between px-5 sm:px-8"><AppMark href="/dashboard" /><div className="flex items-center gap-1.5"><ThemeToggle /><div className="hidden items-center gap-2 border-l border-slate-200 pl-3 sm:flex dark:border-white/10"><div className="grid size-7 place-items-center rounded-full bg-violet-100 text-xs font-semibold text-violet-800 dark:bg-violet-400/15 dark:text-violet-200">{user?.name?.slice(0,1).toUpperCase() || "P"}</div><Button variant="ghost" onClick={logout} className="h-8 rounded-full px-3 text-xs text-slate-600 hover:text-slate-950 dark:text-slate-300 dark:hover:text-white">Log out</Button></div></div></div></header><div className="mx-auto grid max-w-7xl gap-8 px-5 py-7 sm:px-8 lg:grid-cols-[13rem_minmax(0,1fr)] lg:py-10"><aside className="hidden lg:block"><nav className="sticky top-28 space-y-1"><a href="/dashboard" className="flex h-10 items-center gap-3 rounded-xl bg-white px-3 text-sm font-semibold text-slate-950 shadow-sm dark:bg-white/10 dark:text-white"><LayoutDashboard className="size-4 text-violet-600 dark:text-violet-300" />Portfolios</a><div className="mt-7 rounded-2xl border border-violet-100 bg-violet-50/70 p-4 dark:border-violet-400/15 dark:bg-violet-950/20"><Sparkles className="size-4 text-violet-700 dark:text-violet-300" /><p className="mt-3 text-xs font-semibold text-violet-950 dark:text-violet-100">Make it unmistakably yours.</p><p className="mt-1 text-xs leading-5 text-violet-800/70 dark:text-violet-200/60">Add a short bio, a memorable URL and visual language.</p></div></nav></aside><main><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-violet-700 dark:text-violet-300">Your workspace</p><h1 className="mt-2 font-display text-4xl font-semibold tracking-[-0.06em] sm:text-5xl">Portfolios</h1><p className="mt-3 text-sm text-slate-500 dark:text-slate-400">{portfolios.length ? `${portfolios.length} ${portfolios.length === 1 ? "portfolio" : "portfolios"} in your studio.` : "A home for the work you want to be remembered by."}</p></div><Button onClick={createPortfolio} disabled={creating} className="h-11 rounded-full bg-violet-700 px-5 text-white hover:bg-violet-800 dark:bg-violet-500 dark:hover:bg-violet-400"><Plus className="mr-1.5 size-4" />{creating ? "Creating…" : "New portfolio"}</Button></div>{list.isLoading ? <div className="grid min-h-64 place-items-center"><Loader2 className="size-5 animate-spin text-violet-600" /></div> : list.error ? <div className="mt-10 rounded-2xl border border-destructive/30 bg-destructive/10 p-5 text-sm text-destructive">We couldn’t load your portfolios. Please refresh and try again.</div> : portfolios.length ? <div className="mt-9 grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{portfolios.map((portfolio) => <PortfolioCard key={portfolio.id} portfolio={portfolio} onDelete={(id) => { if (window.confirm("Delete this portfolio? This cannot be undone.")) remove.mutate({ id }); }} />)}</div> : <div className="mt-9 grid min-h-[24rem] place-items-center rounded-[1.75rem] border border-dashed border-slate-300 bg-white/60 p-8 text-center dark:border-white/15 dark:bg-white/[0.025]"><div><div className="mx-auto grid size-14 place-items-center rounded-2xl bg-violet-100 text-violet-700 dark:bg-violet-400/15 dark:text-violet-300"><FolderPlus className="size-6" /></div><h2 className="mt-5 font-display text-2xl font-semibold tracking-[-0.045em]">Start with a clean canvas.</h2><p className="mx-auto mt-2 max-w-sm text-sm leading-6 text-slate-500 dark:text-slate-400">Create your first portfolio, then make it feel like yours in the live editor.</p><Button onClick={createPortfolio} disabled={creating} className="mt-6 rounded-full bg-violet-700 text-white hover:bg-violet-800">Create portfolio</Button></div></div>}</main></div></div>;
}
