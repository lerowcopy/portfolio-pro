import React from "react";
import { ArrowRight } from "lucide-react";
import { ContactForm, ProfileHero, ProjectMedia, SectionHeading, SocialNav, TemplateFooter, type TemplatePortfolio } from "./TemplatePrimitives";

export function BlogTemplate({ portfolio }: { portfolio: TemplatePortfolio }) {
  return <article className="mx-auto max-w-5xl space-y-14 px-5 py-12 sm:px-10 sm:py-16"><ProfileHero portfolio={portfolio} /><SocialNav portfolio={portfolio} /><section aria-labelledby="blog-projects"><SectionHeading title="Latest projects" /><div className="grid gap-5 sm:grid-cols-2">{portfolio.projects.slice(0, 4).map((project, index) => <article key={project.id} className="overflow-hidden rounded-2xl border border-[var(--p-border)] bg-[var(--p-surface)]"><ProjectMedia project={project} index={index} className="aspect-[16/9] w-full" /><div className="p-5"><h3 className="text-lg font-semibold">{project.title}</h3><p className="mt-2 text-sm leading-6 text-[var(--p-muted)]">{project.description}</p></div></article>)}</div></section><section aria-labelledby="blog-posts"><div className="flex items-end justify-between gap-4"><SectionHeading eyebrow="Writing" title="Latest notes" /><a href="#archive" className="mb-6 inline-flex items-center gap-1 text-sm font-semibold text-[var(--p-accent)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--p-accent)]">Archive <ArrowRight className="size-4" /></a></div><div id="archive" className="divide-y divide-[var(--p-border)]">{portfolio.posts.map((post) => <article key={post.id} className="py-5 first:pt-0"><p className="text-xs font-semibold text-[var(--p-muted)]">{post.date}</p><h3 className="mt-2 text-xl font-semibold tracking-[-0.035em]">{post.title}</h3><p className="mt-2 max-w-2xl text-sm leading-6 text-[var(--p-muted)]">{post.excerpt}</p>{post.href ? <a href={post.href} className="mt-3 inline-flex text-sm font-semibold text-[var(--p-accent)]">Read note</a> : null}</article>)}</div></section><ContactForm portfolio={portfolio} title="A good idea deserves a conversation" /><TemplateFooter portfolio={portfolio} /></article>;
}
